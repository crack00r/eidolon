<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>Eidolon</string>

    <key>CFBundleIdentifier</key>
    <string>com.eidolon.ios</string>

    <key>CFBundleVersion</key>
    <string>1</string>

    <key>CFBundleShortVersionString</key>
    <string>0.1.7</string>

    <key>CFBundlePackageType</key>
    <string>APPL</string>

    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>

    <key>LSRequiresIPhoneOS</key>
    <true/>

    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
    </dict>

    <key>UILaunchScreen</key>
    <dict/>

    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- Bonjour service browsing for local network discovery -->
    <key>NSBonjourServices</key>
    <array>
        <string>_eidolon._tcp</string>
    </array>

    <key>NSLocalNetworkUsageDescription</key>
    <string>Eidolon uses the local network to discover and connect to the Eidolon Core server running on your LAN.</string>

    <!-- Microphone access for voice mode -->
    <key>NSMicrophoneUsageDescription</key>
    <string>Eidolon needs microphone access to enable voice conversations with the AI assistant.</string>

    <!-- Speech recognition (reserved for future on-device VAD) -->
    <key>NSSpeechRecognitionUsageDescription</key>
    <string>Eidolon may use speech recognition for voice activity detection.</string>

    <!-- Background audio for voice mode -->
    <key>UIBackgroundModes</key>
    <array>
        <string>audio</string>
    </array>

    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSExceptionDomains</key>
        <dict>
            <key>localhost</key>
            <dict>
                <key>NSExceptionAllowsInsecureHTTPLoads</key>
                <true/>
            </dict>
        </dict>
    </dict>
</dict>
</plist>
